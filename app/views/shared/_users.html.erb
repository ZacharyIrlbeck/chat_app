<div id="userList" class="col-4">
	<% User.active.each do |user| %>
	  <%= render partial: "shared/user", locals: {user: user} if user != current_user %>
	<% end %>
</div>

<%= javascript_include_tag "users" %>